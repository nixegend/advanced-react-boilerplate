// @flow
import React from 'react';

const propTypes = {};
const defaultProps = {};

const About = (props: Object): Object => (
  <div className='about'>
    <h2>About page content</h2>
  </div>
);

About.propTypes = propTypes;
About.defaultProps = defaultProps;

export default About;
